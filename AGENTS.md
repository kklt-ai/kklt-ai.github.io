# AGENTS.md

本文件适用于整个仓库，供在本项目中工作的自动化代理和协作者使用。
并与本项目约定合并；如有冲突，以更具体的项目约定和用户要求为准。

## 项目概况

- 这是一个 Astro 5 纯静态博客，使用 npm 管理依赖。
- 页面、布局和样式位于 `src/`；公开静态资源位于 `public/`。
- 公众号文章和站内 Markdown 文章由 `src/data/articles.ts` 统一汇总，并按日期倒序排列。
- 项目目前没有独立的测试或 lint 脚本，`npm run build` 是主要验收命令。

## 常用命令

```bash
npm ci
npm run dev
npm run build
npm run preview
```

仅在需要重新抓取现有公众号文章信息时运行：

```bash
node scripts/fetch-wechat-articles.mjs
```

该脚本会覆盖 `src/data/wechat-articles.ts` 并更新 `public/covers/` 中的封面，运行后必须检查差异。

## 目录职责

- `src/pages/`：Astro 文件路由。首页、项目页、文章列表和详情页均在此维护。
- `src/layouts/BaseLayout.astro`：全站基础结构、导航和页脚。
- `src/styles/global.css`：全局设计变量、共享样式和动画。
- `src/content/blog/`：站内 Markdown 文章。
- `src/content.config.ts`：Markdown frontmatter schema。
- `src/data/projects.ts`：项目数据和首页精选逻辑。
- `src/data/wechat-articles.ts`：公众号文章元数据。
- `src/data/articles.ts`：两类文章的统一模型、合并和排序逻辑。
- `src/assets/`：通过 Astro import 并参与构建优化的资源。
- `public/`：按原路径公开的静态资源。
- `scripts/`：一次性维护脚本。

不要直接编辑 `dist/`、`.astro/` 或 `node_modules/`；它们是构建或依赖生成目录。

## 工作原则

以下原则偏向谨慎和准确；对于无歧义的小改动，可按任务规模简化过程。

### 1. 编码前先想清楚

- 开始前明确任务目标、约束、假设和可验证的完成标准，不隐瞒不确定性。
- 如果存在多种合理理解，先说明差异和影响，不要静默选择其中一种。
- 如果信息不足会明显改变实现方向，指出缺失信息并询问；不要依靠猜测继续。
- 如果有明显更简单的实现，主动说明并优先采用；需求会引入不必要复杂度时，应解释代价。
- 修改前检查相关源码、配置和 `git status`，不要仅凭文件名或记忆推断行为。

### 2. 简单优先

- 编写解决当前问题所需的最少代码，不添加未要求的功能。
- 不为单次使用创建抽象，不增加没有明确需求的灵活性、配置项或兼容层。
- 不为实际不可能发生的情况增加防御代码；错误处理应对应真实边界。
- 如果实现明显长于问题本身，停下来寻找更直接的方案。
- 优先复用现有数据接口、设计变量和项目模式。
- 新增依赖必须有明确必要性，并同步提交 `package.json` 与 `package-lock.json`。

### 3. 手术式修改

- 每一处改动都应能直接追溯到当前任务，只修改完成任务所需的文件和代码。
- 不顺带重构、改名、重新格式化或“改进”相邻代码、注释和样式。
- 遵循现有 Astro、TypeScript 和 CSS 风格，即使个人偏好不同。
- 单个页面专用样式继续放在页面的 `<style>` 中，共享样式才放入 `global.css`。
- 保留工作区中已有的用户变更；若与任务冲突，先说明冲突，不覆盖或回退。
- 如果发现无关的废弃代码，只在交付时说明，不主动删除。
- 删除由本次修改造成的未使用 import、变量、函数或文件，但不清理原先就存在的孤立代码。
- 除非任务明确要求，不修改页面文案、URL 结构、视觉语言或文章内容。

### 4. 目标驱动执行

- 把模糊要求转成可检查的结果，例如“修复问题”应对应可复现的问题和修复后的验证。
- 多步骤任务先给出简短计划，每一步同时写明验证方式：

  ```text
  1. [步骤] → 验证：[检查]
  2. [步骤] → 验证：[检查]
  3. [步骤] → 验证：[检查]
  ```

- 修复 bug 时，有测试基础设施就先补充失败用例；没有时至少保留最小复现步骤。
- 重构前后运行相同验证，确保行为没有意外变化。
- 实现后持续检查并修正，直到完成标准满足；不要以“应该可用”代替验证结果。

## 内容与资源约定

- Markdown frontmatter 必须符合 `src/content.config.ts`：`title`、`description`、`pubDate` 必填，`tags` 默认为空数组，`cover` 可选。
- 文章文件名即 URL slug；改名会改变公开链接，未经明确要求不要修改。
- 日期统一使用 `YYYY-MM-DD`，以保证字符串排序与展示结果一致。
- 公众号封面放在 `public/covers/`，引用路径写成 `/covers/<文件名>`。
- 站内文章配图优先放在 `public/blog/<slug>/`，引用路径写成 `/blog/<slug>/<文件名>`。
- 需要 Astro 图片优化的页面资源放在 `src/assets/`，并通过 import 使用。
- 项目颜色只能使用 `green`、`orange`、`blue`、`pink`；首页精选由 `featured: true` 控制，当前最多展示 3 个。

## 验证与交付

完成代码、内容或配置修改后：

1. 运行 `npm run build`。
2. 检查构建是否生成预期路由，且没有 Content Collections 校验错误。
3. 使用 `git diff --check` 检查空白错误。
4. 使用 `git status --short` 和 `git diff` 确认只包含任务相关变更。

如果仅修改文档，也至少检查文档中的路径、命令和目录树是否与仓库现状一致。若因环境问题无法运行验证命令，应在交付说明中明确指出。
